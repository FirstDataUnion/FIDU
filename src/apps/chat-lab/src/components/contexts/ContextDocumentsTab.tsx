import { Box, Typography } from '@mui/material';

export default function ContextDocumentsTab() {
  return (
    <Box>
      <Typography variant="body1" color="text.secondary">
        Documents are files that you can add to conversations directly or via a
        corpus.
      </Typography>
    </Box>
  );
}
